name: presenter_state
description: State Manager
version: 0.0.1
homepage:

environment:
  sdk: '>=3.1.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  mocktail: ^1.0.4
  provider: ^6.1.5

dev_dependencies:
  test_cov_console: ^0.2.2
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  lints: ^5.1.1
  test_coverage_badge: ^0.3.2

