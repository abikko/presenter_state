name: presenter_state
description: State Manager
version: 0.0.1
homepage:

environment:
  sdk: '>=3.1.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  mocktail: ^1.0.4

dev_dependencies:
  test_cov_console: ^0.2.2
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

